[toolchain]

[features]
resolution = true
skip-lint = false
 

[registry]
url = "https://api.apr.dev"

[provider]
cluster = "Localnet"

wallet = "wallet.json"

[scripts]
test = "pnpm ts-mocha -p ./tsconfig.json -t 1000000 tests/**/*.ts"
